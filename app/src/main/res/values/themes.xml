<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Assign1" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/primary_brand</item>
        <item name="colorSecondary">@color/accent_emerald</item>
        <item name="android:windowBackground">@color/background_soft</item>
        <item name="android:statusBarColor">@color/primary_brand</item>
    </style>

    <style name="Theme.Assign1" parent="Base.Theme.Assign1" />

    <style name="Theme.Assign1.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/background_soft</item>
        <item name="android:statusBarColor">@color/primary_brand</item>
    </style>
</resources>